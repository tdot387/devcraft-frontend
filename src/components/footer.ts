import { renderFooterTemplate } from '../templates/footer.template';

export function renderFooter(): string {
  const template = renderFooterTemplate();
  // javascript logic here if needed

  return template;
}
