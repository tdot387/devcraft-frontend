import { renderLoadingSpinnerTemplate } from '../templates/loadingSpinner.template';

export function renderLoadingSpinner(): string {
  const template = renderLoadingSpinnerTemplate();

  return template;
}
