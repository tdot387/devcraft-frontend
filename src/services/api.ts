// API-Zugriff (fetch, endpoints, error handling)
// TODO: API implementieren

// import { mockRecipes } from './mock/mockData';

// just a placeholder for future API calls to firebase or other backend services
export const apiService = {
  // getRecipes: async () => {
  //   return mockRecipes;
  // },

  // getRecipe: async (id: string) => {
  //   return mockRecipes.find((recipe) => recipe.id === id) || null;
  // },
};
